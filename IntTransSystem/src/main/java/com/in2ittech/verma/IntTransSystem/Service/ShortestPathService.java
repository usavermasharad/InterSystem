package com.in2ittech.verma.IntTransSystem.Service;

import java.util.ArrayList;
import java.util.Collections;
import java.util.HashMap;
import java.util.HashSet;
import java.util.LinkedList;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.springframework.stereotype.Service;

import com.in2ittech.verma.IntTransSystem.entity.Edge;
import com.in2ittech.verma.IntTransSystem.entity.Vertice;
import com.in2ittech.verma.IntTransSystem.utility.Graph;

@Service
public class ShortestPathService {

	public ShortestPathService() {
	}

	/*
	 * public ShortestPathService(Graph graph) { this.vertices = new
	 * ArrayList<>(graph.getVerticees()); if (graph.isTrafficAllowed()) {
	 * graph.processTraffics(); } if (graph.isUndirectedGraph()) { this.edges = new
	 * ArrayList<>(graph.getUndirectedEdges()); } else { this.edges = new
	 * ArrayList<>(graph.getEdges()); } }
	 */

}
